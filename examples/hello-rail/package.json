{
  "name": "hello-rail",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "pnpm build && playwright test"
  },
  "dependencies": {
    "@prism-tv/core": "workspace:*",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "vite": "^7.1.7",
    "vite-plugin-solid": "^2.8.0"
  }
}
